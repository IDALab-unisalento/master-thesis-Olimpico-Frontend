export interface Hospital {
    id: number
    name: string
    country: string
    city: string
    province: string
    address: string
    postal_code: string
    phone_number: string
    email: string
}